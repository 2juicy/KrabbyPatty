<h1>Noms!</h1>

<h2>Krabby Patties that are not eaten!</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  <li>{{id}}. {{burger_name}}</li>
  <button href="/devour/{{id}}" class="changeEaten">Devour Krabby Patty</button>
  {{/unless}}
  {{/each}}
</ul>

<h2>Krabby Patties that are devoured!</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  <li>{{id}}. {{burger_name}}</li>
  {{/if}}
  {{/each}}
</ul>

<h2>Create your own Krabby Patty!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">Burger Name: </label>
    <input type="text" id="burg" name="name">
  </div>

  <button href="/add" type="submit">Add Krabby Patty</button>
</form>